'use client';

import { DocumentScanner } from '@/components';

const ScannerPage = () => {
    return (
        <div>
            <h1>Scan Documents</h1>
            <DocumentScanner />
        </div>
    );
};

export default ScannerPage;
