import React from 'react'

import QrCodeScanner from '@/components/qr/barcodeScanner'

const QR = (props) => {
    return (
        <div>
            <QrCodeScanner />
        </div>
    );
};

export default QR;